--- crates/worktree/src/infrastructure/persistence/worktree_repository_sqlx.rs
+++ replace <impl WorktreeRepository for SqlxWorktreeRepository>::get_by_id -> Result<Option<Worktree>, WorktreeRepositoryError> with Ok(Some(Default::default()))
@@ -119,38 +119,17 @@
                          }
                      }
                  }
             })
         })
     }
 
     fn get_by_id(&self, id: &WorktreeId) -> Result<Option<Worktree>, WorktreeRepositoryError> {
-        let pool = self.pool.clone();
-        let id_str = id.to_string();
-
-        tokio::task::block_in_place(|| {
-            tokio::runtime::Handle::current().block_on(async move {
-                let sql = r#"
-                    SELECT id, name, worktree_type, path, state, source_branch, description, created_at
-                    FROM worktrees WHERE id = ?
-                "#;
-
-               let result = sqlx::query_as::<_, WorktreeRow>(sql)
-                     .bind(id_str)
-                     .fetch_optional(pool.as_ref())
-                     .await
-                     .map_err(|e| WorktreeRepositoryError::DatabaseError(e.to_string()))?;
-
-                 match result {
-                     Some(row) => row.to_worktree().map(Some),
-                     None => Ok(None),
-                 }
-            })
-        })
+        Ok(Some(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     fn get_by_name(
         &self,
         name: &WorktreeName,
     ) -> Result<Option<Worktree>, WorktreeRepositoryError> {
         let pool = self.pool.clone();
         let name_str = name.to_string();
