--- crates/worktree/src/domain/entities/worktree.rs
+++ replace Worktree::with_id -> Self with Default::default()
@@ -210,26 +210,17 @@
         id: WorktreeId,
         name: WorktreeName,
         worktree_type: WorktreeTypeEnum,
         path: AbsolutePath,
         source_branch: BranchName,
         created_at: DateTime<Utc>,
         description: Option<String>,
     ) -> Self {
-        Self {
-            id,
-            name,
-            worktree_type,
-            path,
-            state: WorktreeState::Created,
-            created_at,
-            source_branch,
-            description,
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Activate this worktree.
     ///
     /// Returns a new Worktree with the updated state.
     pub fn activate(self) -> Option<Self> {
         self.state.activate().map(|state| Self { state, ..self })
     }
