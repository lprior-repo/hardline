--- crates/worktree/src/domain/entities/worktree_id.rs
+++ replace <impl serde::Serialize for WorktreeId>::serialize -> Result<S::Ok, S::Error> with Ok(Default::default())
@@ -65,17 +65,17 @@
     }
 }
 
 impl serde::Serialize for WorktreeId {
     fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
     where
         S: serde::Serializer,
     {
-        self.0.serialize(serializer)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl<'de> serde::Deserialize<'de> for WorktreeId {
     fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
     where
         D: serde::Deserializer<'de>,
     {
