--- crates/worktree/src/domain/entities/worktree_id.rs
+++ replace WorktreeId::to_string -> String with "xyzzy".into()
@@ -37,17 +37,17 @@
 
     /// Get the inner UUID.
     pub fn as_uuid(&self) -> Uuid {
         self.0
     }
 
     /// Convert to a string representation.
     pub fn to_string(&self) -> String {
-        self.0.to_string()
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Check if this is the default/empty ID (for testing).
     #[cfg(test)]
     pub(crate) fn is_default(&self) -> bool {
         self.0 == Uuid::nil()
     }
 }
