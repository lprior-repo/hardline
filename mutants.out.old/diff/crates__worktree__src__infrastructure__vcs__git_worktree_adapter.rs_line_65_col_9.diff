--- crates/worktree/src/infrastructure/vcs/git_worktree_adapter.rs
+++ replace GitWorktreeAdapter::execute_git -> Result<String, GitAdapterError> with Ok(String::new())
@@ -57,33 +57,17 @@
 
     /// Get the repository path.
     pub fn repo_path(&self) -> &str {
         &self.repo_path
     }
 
     /// Execute a Git command and return the result.
     fn execute_git(&self, args: &[&str]) -> Result<String, GitAdapterError> {
-        let output = Command::new("git")
-            .args(args)
-            .current_dir(&self.repo_path)
-            .output()
-            .map_err(|e| {
-                GitAdapterError::CommandFailed(format!("Failed to execute git command: {}", e))
-            })?;
-
-        if !output.status.success() {
-            let stderr = String::from_utf8_lossy(&output.stderr);
-            return Err(GitAdapterError::CommandFailed(format!(
-                "Git command failed: {}",
-                stderr.trim()
-            )));
-        }
-
-        Ok(String::from_utf8_lossy(&output.stdout).to_string())
+        Ok(String::new()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl GitAdapter for GitWorktreeAdapter {
     fn add_worktree(&self, path: &str, source_branch: &str) -> Result<String, GitAdapterError> {
         // Validate path is absolute
         if !path.starts_with('/') {
             return Err(GitAdapterError::PathExists {
