--- crates/worktree/src/domain/entities/worktree.rs
+++ replace Worktree::activate -> Option<Self> with None
@@ -226,17 +226,17 @@
             description,
         }
     }
 
     /// Activate this worktree.
     ///
     /// Returns a new Worktree with the updated state.
     pub fn activate(self) -> Option<Self> {
-        self.state.activate().map(|state| Self { state, ..self })
+        None /* ~ changed by cargo-mutants ~ */
     }
 
     /// Deactivate this worktree.
     ///
     /// Returns a new Worktree with the updated state.
     pub fn deactivate(self) -> Option<Self> {
         self.state.deactivate().map(|state| Self { state, ..self })
     }
