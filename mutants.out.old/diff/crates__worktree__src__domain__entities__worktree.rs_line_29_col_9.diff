--- crates/worktree/src/domain/entities/worktree.rs
+++ replace AbsolutePath::parse -> Result<Self, AbsolutePathParseError> with Ok(Default::default())
@@ -21,21 +21,17 @@
 pub struct AbsolutePath(String);
 
 impl AbsolutePath {
     /// Parse a string into an AbsolutePath.
     ///
     /// # Errors
     /// Returns `AbsolutePathParseError` if the path is not absolute.
     pub fn parse(s: &str) -> Result<Self, AbsolutePathParseError> {
-        if s.starts_with('/') {
-            Ok(Self(s.to_string()))
-        } else {
-            Err(AbsolutePathParseError::NotAbsolute)
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Create an AbsolutePath without validation.
     ///
     /// # Safety
     /// Caller must ensure the path is absolute.
     #[allow(dead_code)]
     pub(crate) fn unsafe_new(s: String) -> Self {
