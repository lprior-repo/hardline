--- crates/worktree/src/domain/entities/worktree.rs
+++ replace Worktree::path_str -> &str with "xyzzy"
@@ -268,17 +268,17 @@
 
     /// Get the worktree name as a string slice.
     pub fn name_str(&self) -> &str {
         self.name.as_str()
     }
 
     /// Get the path as a string slice.
     pub fn path_str(&self) -> &str {
-        self.path.as_str()
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 
     /// Get the source branch as a string slice.
     pub fn source_branch_str(&self) -> &str {
         self.source_branch.as_str()
     }
 }
 
