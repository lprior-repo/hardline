--- crates/worktree/src/domain/repositories/worktree_repository.rs
+++ replace <impl WorktreeRepository for InMemoryWorktreeRepository>::delete -> Result<(), WorktreeRepositoryError> with Ok(())
@@ -352,27 +352,17 @@
             .worktrees
             .read()
             .map_err(|e| WorktreeRepositoryError::DatabaseError(e.to_string()))?;
 
         Ok(filter.apply(worktrees.values()).cloned().collect())
     }
 
     fn delete(&self, id: &WorktreeId) -> Result<(), WorktreeRepositoryError> {
-        let mut worktrees = self
-            .worktrees
-            .write()
-            .map_err(|e| WorktreeRepositoryError::DatabaseError(e.to_string()))?;
-
-        if !worktrees.contains_key(id) {
-            return Err(WorktreeRepositoryError::not_found(id.to_string().as_str()));
-        }
-
-        worktrees.remove(id);
-        Ok(())
+        Ok(()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn update_state(
         &self,
         id: &WorktreeId,
         state: WorktreeState,
     ) -> Result<(), WorktreeRepositoryError> {
         let mut worktrees = self
