--- crates/worktree/src/domain/entities/worktree_type.rs
+++ replace WorktreeTypeEnum::parse -> Result<Self, WorktreeTypeParseError> with Ok(Default::default())
@@ -99,21 +99,17 @@
 pub enum WorktreeTypeEnum {
     Dev,
     Agent,
 }
 
 impl WorktreeTypeEnum {
     /// Parse a worktree type from string.
     pub fn parse(s: &str) -> Result<Self, WorktreeTypeParseError> {
-        match s.to_lowercase().as_str() {
-            "dev" => Ok(Self::Dev),
-            "agent" => Ok(Self::Agent),
-            other => Err(WorktreeTypeParseError::new(other.to_string())),
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Get the string representation.
     pub fn as_str(&self) -> &'static str {
         match self {
             Self::Dev => "dev",
             Self::Agent => "agent",
         }
