--- crates/worktree/src/domain/repositories/worktree_repository.rs
+++ replace ListFilter::apply -> impl Iterator<Item =&'a Worktree>+'b with ::std::iter::empty()
@@ -69,23 +69,17 @@
         self
     }
 
     /// Apply the filter to a collection of worktrees.
     pub fn apply<'b, 'a, I>(&'b self, worktrees: I) -> impl Iterator<Item = &'a Worktree> + 'b
     where
         I: Iterator<Item = &'a Worktree> + 'b,
     {
-        let limit = self.limit.unwrap_or(usize::MAX);
-        let offset = self.offset.unwrap_or(0);
-
-        worktrees
-            .filter(move |w| self.matches(w))
-            .skip(offset)
-            .take(limit)
+        ::std::iter::empty() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Check if a worktree matches this filter.
     fn matches(&self, worktree: &Worktree) -> bool {
         if let Some(ref worktree_type) = self.worktree_type {
             if worktree.worktree_type != *worktree_type {
                 return false;
             }
