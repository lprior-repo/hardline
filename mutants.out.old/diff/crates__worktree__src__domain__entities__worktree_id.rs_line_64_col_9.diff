--- crates/worktree/src/domain/entities/worktree_id.rs
+++ replace <impl fmt::Display for WorktreeId>::fmt -> fmt::Result with Ok(Default::default())
@@ -56,17 +56,17 @@
     fn default() -> Self {
         // Use nil UUID as default for testing
         Self(Uuid::nil())
     }
 }
 
 impl fmt::Display for WorktreeId {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        write!(f, "{}", self.0)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl serde::Serialize for WorktreeId {
     fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
     where
         S: serde::Serializer,
     {
