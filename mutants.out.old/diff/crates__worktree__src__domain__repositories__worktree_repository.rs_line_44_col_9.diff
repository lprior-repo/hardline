--- crates/worktree/src/domain/repositories/worktree_repository.rs
+++ replace ListFilter::with_worktree_type -> Self with Default::default()
@@ -36,18 +36,17 @@
 impl ListFilter {
     /// Create a new empty filter.
     pub fn new() -> Self {
         Self::default()
     }
 
     /// Set the worktree type filter.
     pub fn with_worktree_type(mut self, worktree_type: WorktreeTypeEnum) -> Self {
-        self.worktree_type = Some(worktree_type);
-        self
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Set the state filter.
     pub fn with_state(mut self, state: WorktreeState) -> Self {
         self.state = Some(state);
         self
     }
 
