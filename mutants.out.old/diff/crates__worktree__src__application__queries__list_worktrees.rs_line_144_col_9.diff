--- crates/worktree/src/application/queries/list_worktrees.rs
+++ replace ListWorktreesQuery::for_testing -> Self with Default::default()
@@ -136,24 +136,17 @@
     /// Create a query with in-memory repository (for testing).
     pub fn for_testing(
         worktree_type: Option<WorktreeTypeEnum>,
         state: Option<crate::domain::entities::WorktreeState>,
         name_prefix: Option<String>,
         limit: Option<usize>,
         offset: Option<usize>,
     ) -> Self {
-        Self {
-            worktree_type,
-            state,
-            name_prefix,
-            limit,
-            offset,
-            repository: Box::new(InMemoryWorktreeRepository::new()),
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::{
         AbsolutePath, BranchName, Worktree, WorktreeId, WorktreeName, WorktreeState,
