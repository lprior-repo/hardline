--- crates/worktree/src/application/commands/create_worktree.rs
+++ delete ! in CreateWorktreeCommand::execute
@@ -314,17 +314,17 @@
     /// Execute the command to create a worktree.
     pub fn execute(
         self,
     ) -> Result<crate::domain::entities::worktree_id::WorktreeId, CreateWorktreeError> {
         // Validate worktree name
         let name = crate::domain::entities::worktree_name::WorktreeName::parse(&self.name)?;
 
         // Validate path is absolute
-        if !self.path.starts_with('/') {
+        if  /* ~ changed by cargo-mutants ~ */self.path.starts_with('/') {
             return Err(CreateWorktreeError::PathNotAbsolute {
                 path: self.path.clone(),
             });
         }
 
         // Validate branch name
         let source_branch =
             crate::domain::entities::worktree::BranchName::parse(&self.source_branch)?;
