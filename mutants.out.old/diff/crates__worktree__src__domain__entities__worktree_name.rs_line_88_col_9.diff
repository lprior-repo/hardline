--- crates/worktree/src/domain/entities/worktree_name.rs
+++ replace <impl fmt::Display for WorktreeName>::fmt -> fmt::Result with Ok(Default::default())
@@ -80,17 +80,17 @@
     /// Check if the name is empty.
     pub fn is_empty(&self) -> bool {
         self.0.is_empty()
     }
 }
 
 impl fmt::Display for WorktreeName {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        write!(f, "{}", self.0)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl AsRef<str> for WorktreeName {
     fn as_ref(&self) -> &str {
         &self.0
     }
 }
