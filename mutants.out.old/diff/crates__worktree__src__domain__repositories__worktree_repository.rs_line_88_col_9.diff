--- crates/worktree/src/domain/repositories/worktree_repository.rs
+++ replace ListFilter::matches -> bool with true
@@ -80,35 +80,17 @@
         worktrees
             .filter(move |w| self.matches(w))
             .skip(offset)
             .take(limit)
     }
 
     /// Check if a worktree matches this filter.
     fn matches(&self, worktree: &Worktree) -> bool {
-        if let Some(ref worktree_type) = self.worktree_type {
-            if worktree.worktree_type != *worktree_type {
-                return false;
-            }
-        }
-
-        if let Some(state) = self.state {
-            if worktree.state != state {
-                return false;
-            }
-        }
-
-        if let Some(ref prefix) = self.name_prefix {
-            if !worktree.name_str().starts_with(prefix) {
-                return false;
-            }
-        }
-
-        true
+        true /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Repository trait for Worktree persistence.
 ///
 /// This trait defines the interface for persisting and retrieving worktrees.
 /// Implementations can use any persistence mechanism (SQL, NoSQL, in-memory, etc.).
 pub trait WorktreeRepository: Send + Sync + std::fmt::Debug {
