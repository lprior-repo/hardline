--- crates/worktree/src/domain/entities/worktree_type.rs
+++ replace <impl WorktreeType for Agent>::to_string -> String with "xyzzy".into()
@@ -72,17 +72,17 @@
         if s.eq_ignore_ascii_case("agent") {
             Ok(Self)
         } else {
             Err(WorktreeTypeParseError::InvalidType(s.to_string()))
         }
     }
 
     fn to_string(&self) -> String {
-        Self::type_name().to_string()
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Error types for WorktreeType parsing.
 #[derive(Debug, Clone, PartialEq, Eq, thiserror::Error)]
 pub enum WorktreeTypeParseError {
     #[error("Invalid worktree type: {0}. Must be 'dev' or 'agent'")]
     InvalidType(String),
