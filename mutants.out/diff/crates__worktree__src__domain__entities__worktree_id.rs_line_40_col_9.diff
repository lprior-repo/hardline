--- crates/worktree/src/domain/entities/worktree_id.rs
+++ replace WorktreeId::as_uuid -> Uuid with Default::default()
@@ -32,17 +32,17 @@
     pub fn parse(s: &str) -> Result<Self, WorktreeIdParseError> {
         Uuid::parse_str(s)
             .map(Self)
             .map_err(|_| WorktreeIdParseError::InvalidUuid)
     }
 
     /// Get the inner UUID.
     pub fn as_uuid(&self) -> Uuid {
-        self.0
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     /// Convert to a string representation.
     pub fn to_string(&self) -> String {
         self.0.to_string()
     }
 
     /// Check if this is the default/empty ID (for testing).
