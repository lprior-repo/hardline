--- crates/worktree/src/application/commands/create_worktree.rs
+++ replace <impl GitAdapter for MockGitAdapter>::remove_worktree -> Result<(), GitAdapterError> with Ok(())
@@ -29,17 +29,17 @@
 }
 
 impl GitAdapter for MockGitAdapter {
     fn add_worktree(&self, _path: &str, _source_branch: &str) -> Result<String, GitAdapterError> {
         self.add_worktree_result.clone()
     }
 
     fn remove_worktree(&self, _path: &str) -> Result<(), GitAdapterError> {
-        self.remove_worktree_result.clone()
+        Ok(()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Command to create a new worktree.
 ///
 /// This command encapsulates all the logic required to create a worktree,
 /// including validation, persistence, and Git operations.
 #[derive(Debug)]
