--- crates/worktree/src/domain/repositories/worktree_repository.rs
+++ replace <impl WorktreeRepository for InMemoryWorktreeRepository>::add -> Result<WorktreeId, WorktreeRepositoryError> with Ok(Default::default())
@@ -296,37 +296,17 @@
             .map(|lock| lock.values().cloned().collect())
             .unwrap_or_else(|| Vec::new())
     }
 }
 
 impl WorktreeRepository for InMemoryWorktreeRepository {
     fn add(&self, worktree: &Worktree) -> Result<WorktreeId, WorktreeRepositoryError> {
         // Check for duplicate name
-        let name = worktree.name_str().to_string();
-        let worktrees = self
-            .worktrees
-            .read()
-            .map_err(|e| WorktreeRepositoryError::DatabaseError(e.to_string()))?;
-
-        if worktrees.values().any(|w| w.name_str() == name) {
-            return Err(WorktreeRepositoryError::duplicate_name(&name));
-        }
-
-        drop(worktrees);
-
-        let mut worktrees = self
-            .worktrees
-            .write()
-            .map_err(|e| WorktreeRepositoryError::DatabaseError(e.to_string()))?;
-
-        let id = worktree.id.clone();
-        worktrees.insert(id.clone(), worktree.clone());
-
-        Ok(id)
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn get_by_id(&self, id: &WorktreeId) -> Result<Option<Worktree>, WorktreeRepositoryError> {
         let worktrees = self
             .worktrees
             .read()
             .map_err(|e| WorktreeRepositoryError::DatabaseError(e.to_string()))?;
         Ok(worktrees.get(id).cloned())
