--- crates/worktree/src/domain/entities/worktree_state.rs
+++ replace WorktreeState::is_created -> bool with false
@@ -27,17 +27,17 @@
 impl WorktreeState {
     /// Check if the worktree is currently active.
     pub fn is_active(&self) -> bool {
         matches!(self, Self::Active)
     }
 
     /// Check if the worktree has been created (either active or just created).
     pub fn is_created(&self) -> bool {
-        matches!(self, Self::Created | Self::Active)
+        false /* ~ changed by cargo-mutants ~ */
     }
 
     /// Transition to Active state.
     ///
     /// Returns None if already active.
     pub fn activate(self) -> Option<Self> {
         if self.is_active() {
             None
