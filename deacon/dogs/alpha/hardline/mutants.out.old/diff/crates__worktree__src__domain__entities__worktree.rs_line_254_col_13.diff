--- crates/worktree/src/domain/entities/worktree.rs
+++ delete field description from struct Self expression in Worktree::with_description
@@ -246,17 +246,17 @@
     /// Returns a new Worktree with the updated state.
     pub fn with_state(self, state: WorktreeState) -> Self {
         Self { state, ..self }
     }
 
     /// Update the description.
     pub fn with_description(self, description: Option<String>) -> Self {
         Self {
-            description,
+             /* ~ changed by cargo-mutants ~ */
             ..self
         }
     }
 
     /// Check if this worktree is active.
     pub fn is_active(&self) -> bool {
         self.state.is_active()
     }
