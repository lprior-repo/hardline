--- crates/worktree/src/infrastructure/vcs/git_worktree_adapter.rs
+++ replace <impl GitAdapter for GitWorktreeAdapter>::add_worktree -> Result<String, GitAdapterError> with Ok("xyzzy".into())
@@ -80,34 +80,17 @@
 
         Ok(String::from_utf8_lossy(&output.stdout).to_string())
     }
 }
 
 impl GitAdapter for GitWorktreeAdapter {
     fn add_worktree(&self, path: &str, source_branch: &str) -> Result<String, GitAdapterError> {
         // Validate path is absolute
-        if !path.starts_with('/') {
-            return Err(GitAdapterError::PathExists {
-                path: path.to_string(),
-            });
-        }
-
-        // Check if path already exists
-        let path_obj = Path::new(path);
-        if path_obj.exists() {
-            return Err(GitAdapterError::PathExists {
-                path: path.to_string(),
-            });
-        }
-
-        // Execute: git worktree add <path> <branch>
-        self.execute_git(&["worktree", "add", path, source_branch])?;
-
-        Ok(path.to_string())
+        Ok("xyzzy".into()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn remove_worktree(&self, path: &str) -> Result<(), GitAdapterError> {
         // Execute: git worktree remove <path>
         self.execute_git(&["worktree", "remove", path])?;
         Ok(())
     }
 }
