--- crates/worktree/src/domain/entities/worktree_name.rs
+++ replace <impl AsRef<str> for WorktreeName>::as_ref -> &str with "xyzzy"
@@ -86,17 +86,17 @@
 impl fmt::Display for WorktreeName {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
         write!(f, "{}", self.0)
     }
 }
 
 impl AsRef<str> for WorktreeName {
     fn as_ref(&self) -> &str {
-        &self.0
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Error types for WorktreeName parsing.
 #[derive(Debug, Clone, PartialEq, Eq, thiserror::Error)]
 pub enum WorktreeNameParseError {
     #[error("Worktree name must be at least {min} characters", min = WorktreeName::MIN_LENGTH)]
     TooShort,
