--- crates/worktree/src/domain/entities/worktree_name.rs
+++ replace WorktreeName::parse -> Result<Self, WorktreeNameParseError> with Ok(Default::default())
@@ -25,42 +25,17 @@
     const MIN_LENGTH: usize = 1;
 
     /// Parse a string into a WorktreeName, validating the format.
     ///
     /// # Errors
     /// Returns `WorktreeNameParseError` if the name is invalid.
     pub fn parse(s: &str) -> Result<Self, WorktreeNameParseError> {
         // Validate length
-        if s.len() < Self::MIN_LENGTH {
-            return Err(WorktreeNameParseError::TooShort);
-        }
-        if s.len() > Self::MAX_LENGTH {
-            return Err(WorktreeNameParseError::TooLong);
-        }
-
-        // Cannot start or end with hyphen
-        if s.starts_with('-') || s.ends_with('-') {
-            return Err(WorktreeNameParseError::InvalidBoundary);
-        }
-
-        // Only allow lowercase alphanumeric and hyphens
-        if !s
-            .chars()
-            .all(|c| c.is_ascii_lowercase() || c.is_ascii_digit() || c == '-')
-        {
-            return Err(WorktreeNameParseError::InvalidCharacters);
-        }
-
-        // Cannot have consecutive hyphens
-        if s.contains("--") {
-            return Err(WorktreeNameParseError::ConsecutiveHyphens);
-        }
-
-        Ok(Self(s.to_string()))
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     /// Create a WorktreeName without validation (for internal use only).
     ///
     /// # Safety
     /// Caller must ensure the string is a valid worktree name.
     #[allow(dead_code)]
     pub(crate) fn unsafe_new(s: String) -> Self {
