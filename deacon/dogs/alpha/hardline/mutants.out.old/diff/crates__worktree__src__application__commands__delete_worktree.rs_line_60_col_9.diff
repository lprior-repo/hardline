--- crates/worktree/src/application/commands/delete_worktree.rs
+++ replace <impl From<crate::domain::repositories::worktree_repository::WorktreeRepositoryError> for DeleteWorktreeError>::from -> Self with Default::default()
@@ -52,17 +52,17 @@
 }
 
 impl From<crate::domain::repositories::worktree_repository::WorktreeRepositoryError>
     for DeleteWorktreeError
 {
     fn from(
         err: crate::domain::repositories::worktree_repository::WorktreeRepositoryError,
     ) -> Self {
-        DeleteWorktreeError::RepositoryError(err.to_string())
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl DeleteWorktreeCommand {
     /// Execute the command to delete a worktree.
     ///
     /// # Returns
     /// `Ok(())` if successful.
