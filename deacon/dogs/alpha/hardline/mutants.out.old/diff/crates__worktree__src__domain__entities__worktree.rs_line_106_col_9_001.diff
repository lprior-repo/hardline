--- crates/worktree/src/domain/entities/worktree.rs
+++ replace BranchName::as_str -> &str with "xyzzy"
@@ -98,17 +98,17 @@
     /// Create a BranchName without validation.
     #[allow(dead_code)]
     pub(crate) fn unsafe_new(s: String) -> Self {
         Self(s)
     }
 
     /// Get the inner string slice.
     pub fn as_str(&self) -> &str {
-        &self.0
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 
     /// Get the length of the branch name.
     pub fn len(&self) -> usize {
         self.0.len()
     }
 
     /// Check if empty.
