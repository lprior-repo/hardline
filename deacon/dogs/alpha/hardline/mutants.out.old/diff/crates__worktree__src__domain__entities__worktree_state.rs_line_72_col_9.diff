--- crates/worktree/src/domain/entities/worktree_state.rs
+++ replace <impl fmt::Display for WorktreeState>::fmt -> fmt::Result with Ok(Default::default())
@@ -64,17 +64,17 @@
             Self::Active => "active",
             Self::Inactive => "inactive",
         }
     }
 }
 
 impl fmt::Display for WorktreeState {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        write!(f, "{}", self.as_str())
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl serde::Serialize for WorktreeState {
     fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
     where
         S: serde::Serializer,
     {
