--- crates/worktree/src/domain/entities/worktree_type.rs
+++ replace <impl WorktreeType for Dev>::from_str -> Result<Self, WorktreeTypeParseError> with Ok(Default::default())
@@ -42,21 +42,17 @@
 pub struct Dev;
 
 impl WorktreeType for Dev {
     fn type_name() -> &'static str {
         "dev"
     }
 
     fn from_str(s: &str) -> Result<Self, WorktreeTypeParseError> {
-        if s.eq_ignore_ascii_case("dev") {
-            Ok(Self)
-        } else {
-            Err(WorktreeTypeParseError::InvalidType(s.to_string()))
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     fn to_string(&self) -> String {
         Self::type_name().to_string()
     }
 }
 
 /// Implementation for Agent worktrees.
