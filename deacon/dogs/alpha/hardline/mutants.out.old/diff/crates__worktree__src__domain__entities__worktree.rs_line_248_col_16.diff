--- crates/worktree/src/domain/entities/worktree.rs
+++ delete field state from struct Self expression in Worktree::with_state
@@ -240,17 +240,17 @@
     pub fn deactivate(self) -> Option<Self> {
         self.state.deactivate().map(|state| Self { state, ..self })
     }
 
     /// Set the state of this worktree.
     ///
     /// Returns a new Worktree with the updated state.
     pub fn with_state(self, state: WorktreeState) -> Self {
-        Self { state, ..self }
+        Self {  /* ~ changed by cargo-mutants ~ */ ..self }
     }
 
     /// Update the description.
     pub fn with_description(self, description: Option<String>) -> Self {
         Self {
             description,
             ..self
         }
