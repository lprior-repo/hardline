--- crates/worktree/src/domain/entities/worktree_state.rs
+++ replace <impl serde::Deserialize<'de> for WorktreeState>::deserialize -> Result<Self, D::Error> with Ok(Default::default())
@@ -82,26 +82,17 @@
     }
 }
 
 impl<'de> serde::Deserialize<'de> for WorktreeState {
     fn deserialize<D>(deserializer: D) -> Result<Self, D::Error>
     where
         D: serde::Deserializer<'de>,
     {
-        let s = String::deserialize(deserializer)?;
-        match s.as_str() {
-            "created" => Ok(Self::Created),
-            "active" => Ok(Self::Active),
-            "inactive" => Ok(Self::Inactive),
-            _ => Err(serde::de::Error::unknown_variant(
-                &s,
-                &["created", "active", "inactive"],
-            )),
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl From<WorktreeState> for &'static str {
     fn from(state: WorktreeState) -> Self {
         state.as_str()
     }
 }
