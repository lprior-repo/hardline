--- crates/worktree/src/infrastructure/vcs/git_worktree_adapter.rs
+++ replace <impl GitAdapter for GitWorktreeAdapter>::remove_worktree -> Result<(), GitAdapterError> with Ok(())
@@ -102,18 +102,17 @@
         // Execute: git worktree add <path> <branch>
         self.execute_git(&["worktree", "add", path, source_branch])?;
 
         Ok(path.to_string())
     }
 
     fn remove_worktree(&self, path: &str) -> Result<(), GitAdapterError> {
         // Execute: git worktree remove <path>
-        self.execute_git(&["worktree", "remove", path])?;
-        Ok(())
+        Ok(()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 /// Adapter that uses git2 library instead of git commands.
 pub struct Git2WorktreeAdapter {
     repo: std::sync::Arc<Repository>,
 }
 
